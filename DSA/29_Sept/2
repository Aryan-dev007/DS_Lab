#include<stdio.h>

long long int fibo_index(int n) {

}

int main() {
    int n;
    printf("Enter the index N\n");
    scanf("%d", &n);
    long long int arr[n] = { 0 };
    for (int i = 0; i < n; i++) {

    }
}
